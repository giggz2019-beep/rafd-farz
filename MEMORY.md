# MEMORY.md — سجل ذاكرة المشروع

هذا الملف هو الذاكرة طويلة المدى لكلود عبر الجلسات. كلود يقرأه في بداية كل جلسة (عبر التعليمات في CLAUDE.md)، **ويضيف عليه سطراً موجزاً بعد كل إنجاز مهم** — إضافة فقط، بدون حذف التاريخ القديم. اكتب الإدخالات الجديدة في الأعلى.

## عن صاحب المشروع

- المالك: giggz (giggz.2019@gmail.com) — يتواصل بالعربي (لهجة سعودية).
- **ليس مبرمجاً** — اشرح له دائماً خطوة بخطوة بلغة بسيطة، بدون مصطلحات تقنية غير ضرورية، وقدّم أوامر جاهزة للنسخ. الصور/اللقطات طريقته المفضلة لعرض المشاكل.
- جهازه: لابتوب Windows، يستخدم VS Code مع إضافة Claude Code (اشتراك claude.ai)، ويستخدم أيضاً كلود كود على الويب (claude.ai/code).
- Git push إلى main يتم يدوياً منه دائماً (قاعدة ثابتة في CLAUDE.md).

## سجل الأعمال (الأحدث أولاً)

### 2026-07-22
- **تسجيل الدخول بـ Google للشركاء**: أُضيف زر "Sign in with Google" في `partner-login.html` مع أكشن `google_login` في `api/partner-auth.js` (تحقق من ID token عبر tokeninfo). معطّل حتى يُضاف `GOOGLE_CLIENT_ID` في مكانين: ثابت في الصفحة + متغير بيئة في Vercel. الفرع: `claude/notebooklm-auth-setup-xilt9g` — **لم يُدمج بعد ولم يُفعَّل client ID**.
- **ربط NotebookLM بكلود كود المحلي**: ثُبّت `notebooklm-mcp-2026` على جهاز المالك (عبر uv) وأُعدّ لـ Claude Code + VS Code Copilot. ملاحظات: يعمل فقط على الجهاز المحلي (ليس جلسات الويب)، جلسة Google تنتهي كل 2–4 أسابيع ويُعاد تفعيلها بـ `notebooklm-mcp-2026 login`، والمالك لم يكن يملك حساب NotebookLM مفعّلاً وقتها (خطأ "Authentication expired" وارد حتى يفتح notebooklm.google.com ويوافق على الشروط).

### أعمال سابقة (من تاريخ git قبل بدء هذا السجل)
- دمج Tamara (تقسيط BNPL) في تسجيل الشركاء وترقية الباقات من لوحة الشريك.
- توحيد الخطوط: العناوين ThmanyahSans والنصوص IBM Plex Sans Arabic.
- نظام الشركاء الكامل: تسجيل بعد الدفع (N-Genius + Tamara)، دخول بكلمة مرور + OTP بريدي، استرجاع كلمة المرور/البريد/اسم المستخدم، محاكاة نفاذ (تجريبية).
- حماية Cloudflare Turnstile على دخول الشركاء (معطّلة حتى يُضاف site key حقيقي).
- راجع `todo.md` لقائمة المهام المفتوحة وملاحظات الاسترجاع القديمة.
